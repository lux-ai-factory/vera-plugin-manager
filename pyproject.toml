[project]
name = "a4s-plugin-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "a4s-plugin-interface>=0.1.0",
]

[tool.uv.sources]
a4s-plugin-interface = { path = "../a4s-plugin-interface", editable = true }
